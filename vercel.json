{
  "buildCommand": "npm run db:generate && npx prisma db push && npm run build",
  "framework": "nextjs",
  "regions": ["fra1"],
  "env": {
    "AI_MOCK_MODE": "true",
    "AUTH_TRUST_HOST": "true"
  }
}
